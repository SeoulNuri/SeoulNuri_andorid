<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hello.seoulnuri">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 우측 상단에 그 현재 위치 버튼을 추가하기 위함 -->
    <application
        android:name=".network.ApplicationController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_app"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <!-- API 9 이상부터 버그가 있어서 이거 추가해줘야함 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB4t8F1Tl2eu2gWU_cCP-881iKHQS-RM-U" />

        <!-- 카카오톡 메타데이터 -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Splash">

        </activity>
        <activity
            android:name=".view.login.LoginActivity"
            android:theme="@style/Login" />
        <activity
            android:name=".view.login.LoginCategoryActivity"
            android:theme="@style/Login" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.hello.seoulnuri.view.planner.PlannerStartActivity"
            android:theme="@style/Login"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name="com.hello.seoulnuri.view.planner.PlannerAddOneActivity" />
        <activity
            android:name="com.hello.seoulnuri.view.planner.PlannerAddFourActivity"
            android:theme="@style/Splash" />

        <activity android:name=".view.planner.PlannerAddPathCheckActivity" />
        <activity android:name=".view.planner.PlannerDateActivity" />
        <activity android:name=".Course_detail" />
        <activity android:name=".info.Info_Detail_Intro" />
        <activity android:name=".view.login.KaKaoSignupActivity" />
        <activity android:name=".SearchActivity"></activity>
        <activity
            android:name=".CourseMapActivity"
            android:label="@string/title_activity_course_map"></activity>
    </application>

</manifest>